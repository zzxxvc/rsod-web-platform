# =============================================================================
# FastAPI Web框架
# =============================================================================
# 必须放在cv前面，否则cv会自动安装高版本numpy导致版本不一致无法运行单图检测
numpy>=1.23.0,<2.0.0
# Web应⽤核⼼框架，提供异步API路由、请求验证等
fastapi>=0.104.0
# ASGI服务器，⽤于运⾏FastAPI应⽤
uvicorn[standard]>=0.24.0
# ⽂件上传⽀持，FastAPI处理multipart/form-data必需
python-multipart>=0.0.6
# 数据验证和配置管理
pydantic>=2.5.0
pydantic-settings>=2.1.0
# JWT 与密码哈希（登录注册）
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# =============================================================================
# 数据库
# =============================================================================
# SQLAlchemy ORM框架
sqlalchemy>=2.0.0
# PostgreSQL数据库驱动
psycopg2-binary>=2.9.0
# =============================================================================
# Redis缓存
# =============================================================================
# Redis Python客户端
redis>=5.0.0
# =============================================================================
# MinIO对象存储
# =============================================================================
# MinIO Python SDK（S3兼容的对象存储）
minio>=7.2.0
# =============================================================================
# YOLO⽬标检测与图像处理
# =============================================================================
# Ultralytics YOLO框架（包含PyTorch）
ultralytics>=8.0.0
# OpenCV（必须完整安装；若 cv2 无 imwrite 请 force-reinstall，勿与 opencv-contrib 共存）
opencv-python==4.11.0.86
# Pillow图像处理库
pillow>=10.0.0
# =============================================================================
# ⼯具库
# =============================================================================
# 环境变量加载
python-dotenv>=1.0.0
# NumPy数值计算（ultralytics⾃动安装，此处指定版本范围）
numpy>=1.23.0,<2.0.0
# JWT 认证依赖
python-jose[cryptography]
passlib
bcrypt
python-jose
passlib[bcrypt]
# OpenAI API客户端库
openai>=1.0.0
#阿里云百炼的官方 Python SDK
dashscope